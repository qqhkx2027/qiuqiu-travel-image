---
name: qiuqiu-travel-image
description: Turn an uploaded travel photo into a separate, source-faithful visual keepsake in one of six curated travel styles. Use for travel-photo stylization, travel postcards, perler companions, sketchbooks, enamel souvenirs, scrapbooks, or map miniatures; not for identity-changing portrait edits.
---

# 秋秋旅行图片

把一张旅行照片，转译成一件值得留下的视觉作品。先保留现场的事实，再选择它被记住的方式。

## 适用范围

- 一张或多张旅行照片，需要逐张生成独立成品。
- 用户指定、或希望推荐六种旅行视觉风格之一。
- 用户只需要可复制提示词、生成成品，或先做风格建议。

不把它用于换脸、改变人物关系、虚构地点，或把多张照片无授权地合为一张。

## 不可妥协的事实

1. 默认一张源照片对应一张成品；多张照片逐张输出，除非用户明确要求拼图。
2. 源照片是人物、地点与空间关系的唯一事实依据。不得增减主体、替换身份、改变姿态、朝向、视线、左右位置、前后关系、视角、透视或关键遮挡。
3. 从照片中只保留 1–3 个地点锚点。地点、年份、坐标或文字未经用户确认时，省略而不猜测。
4. 用户要求“原照不变”、精确比例、固定位置或严格分区时，先保护原图，再生成衍生元素，最后做确定性排版；不要把一次整图生成当作精确合成。
5. 用户只要提示词时，不生成图片；用户明确要求出图时，才调用当前环境可用的图像工具。

若将可识别人像用于公开示例或公开仓库，先确认用户已授权。

## 工作流

### 1. 建立源照片契约

读取照片后，先在心中锁定以下信息；只在需要用户确认时提出问题。

| 维度 | 需要锁定的内容 |
| --- | --- |
| 主体 | 身份、数量、发型或轮廓、服装主色、姿态、动作、视线与互动 |
| 地点 | 最有辨识度的 1–3 个建筑、地形、植物、道路或水岸关系 |
| 构图 | 视角、透视、主体落点、留白方向、遮挡与前后关系 |
| 色彩 | 2–4 个主色，最多 1 个小面积强调色 |
| 文字 | 仅记录用户明确提供且必须出现的地点、年份或短标题 |

### 2. 选择模式

用户点名时直接采用。未点名时，结合照片特点推荐不超过 3 种，并说明各自适合保留什么。

| 模式 | 默认比例 | 最适合保留的瞬间 |
| --- | --- | --- |
| `perler-companion` | 4:5 / 3:4 | 真实照片旁，一个小小的拼豆分身 |
| `minimal-sketchbook` | 1:1 | 大留白中的安静场景与姿态 |
| `watercolor-ticket` | 4:3 横版 | 一张可以寄出或夹进书里的旅行票根 |
| `memory-scrapbook` | 4:5 / 3:4 | 几个最值得记住的动作、材质与碎片 |
| `enamel-souvenir` | 1:1 | 能被压缩成收藏徽章的轮廓与结构 |
| `map-miniature` | 1:1 / 4:5 | 道路、水岸、地形或建筑的空间走势 |

仅加载当前模式需要的说明：[六种模式](references/modes.md)。

### 3. 组装提示词

按以下顺序写：**交付物 → 源照片契约 → 构图 → 材质 → 文字 → 禁止项**。

- 将“高级感”换成可检查的画面条件，例如纸张纤维、缺墨、干刷、短投影、非对称留白和有限色盘。
- 只使用一个模式的配方；不要把拼豆、珐琅、票根和拼贴叠加成一个风格大杂烩。
- 需要可复制提示词时，读取 [提示词配方](references/prompt-recipes.md) 的对应模式，填入源照片契约。

### 4. 生成、检查、修正

生成后按这个顺序验收：

1. **身份与数量**：是不是同一主体，主体数量是否正确。
2. **关系**：姿态、动作、视线、方向、遮挡和前后关系是否保持。
3. **地点**：地点锚点是否可辨认，是否有凭空出现的地标或天气。
4. **版式**：比例、主体大小、分区与留白是否符合要求。
5. **材质**：是否像目标媒介本身，而非简单滤镜、光滑矢量或塑料渲染。
6. **文字与洁净度**：逐字正确；没有乱码、Logo、水印、重复主体或意外拼图。

只修复最小失败项。若人脸、姿势或空间关系连续失败，停止堆叠风格词，回到源照片契约；若精确排版连续失败，改用“衍生元素 + 确定性合成”。详细检查与修正策略见 [质量检查表](references/quality-checklist.md)。

## 输出

- **生成图片**：逐张命名并交付，注明模式与成品比例；不覆盖源文件。
- **只要提示词**：给一段可直接复制的最终提示词，变量不超过 3 个。
- **系列图片**：固定纸张、色盘、材质、文字层级和边距；每张只替换主体与地点锚点。

## 参考资料

- [六种模式](references/modes.md)：构图、比例、材质与禁区。
- [提示词配方](references/prompt-recipes.md)：六段可复制的模式提示词。
- [质量检查表](references/quality-checklist.md)：交付前验收与最小修正策略。
- [案例验证记录](references/model-validation.md)：已观察到的失败方式及处理边界。
